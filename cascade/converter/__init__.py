"""CASCADE Model Converter v0.1 — checkpoint → CASCADE-DIR/0.1."""
from pathlib import Path

CONVERTER_SCRIPT = Path(__file__).resolve().parent / "cascade_converter.py"
FORMAT_NAME = "CASCADE-DIR"
FORMAT_VERSION = "0.1"
