cmake_minimum_required(VERSION 3.16)
project(cascade_runtime LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 20)
add_library(cascade_avx2 STATIC avx2_lowrank.cpp)
target_include_directories(cascade_avx2 PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
if(CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64|AMD64")
  target_compile_options(cascade_avx2 PRIVATE -mavx2 -mfma)
endif()
add_executable(cascade_mmap_smoke mmap_smoke.cpp)
target_include_directories(cascade_mmap_smoke PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
