{
  "name": "observatorio-llm",
  "version": "0.9.0",
  "private": true,
  "description": "RIFT-LM, CASCADE, AETHER, SPECTRA and WINNER benchmark dashboard for Vercel",
  "scripts": {
    "dev": "node scripts/dev_server.mjs",
    "test": "node scripts/battery_group_key_fixtures.mjs && node scripts/dashboard_smoke.mjs && node scripts/real_benchmark_smoke.mjs && node scripts/validate_data.mjs && node scripts/security_check.mjs"
  }
}
